One-section accordion (open/close).

```jsx
<Collapsible title="Backlog" iconName="branch" count={4}>
  <WorkspaceItem …/>
</Collapsible>
```

Chevron rotates 90° when open; keep the header row at 30px so it matches sidebar / menu density.
