Line-icon primitive: pick one of 23 named icons. Stroke inherits from `color`.

```jsx
<Icon name="branch" size={14} />
<Icon name="push" className="glyph" />
```

Variants: any `IconName`. Default `size` is 14 (row density); use 12 for tags/mono, 16 for message-level accents.
