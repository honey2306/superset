User avatar with initials fallback.

```jsx
<Avatar name="Wu Fan" size={24} />
<Avatar name="Claude" src="/claude.png" size={20} />
```

Never draw a colored ring or online-dot around the avatar in the primitive — those are project patterns, add them at the call site.
